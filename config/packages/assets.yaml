framework:
    assets:
        json_manifest_path: '%kernel.project_dir%/public/build/manifest.json'

        packages:
            cdn_package:
                base_urls: [ '%cdn_assets_base_url%' ]
                version: ~

